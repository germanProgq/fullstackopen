sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save
    Note right of browser: SPA handles submit, prevents default

    browser->>browser: Add new note to in-memory list
    browser->>browser: Re-render notes list immediately

    Note right of browser: Send new note to server as JSON (no page reload)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Body: { content: "...", date: "..." }
    server-->>browser: 201 Created (e.g. { message: "note created" })
    deactivate server

    Note right of browser: UI already updated; stays on SPA page

