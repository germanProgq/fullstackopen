{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "Phonebook backend for fullstackopen",
  "license": "ISC",
  "author": "germanProgq",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "update:ui": "cd ../../part2/phonebook && npm run build && cp -r ./dist ../../part3/phonebook_backend",
    "deploy": "fly deploy",
    "deploy:full": "npm run update:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
